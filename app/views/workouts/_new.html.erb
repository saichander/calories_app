<%= form_for(@workout, html: { multipart: true }, class: 'form-horizontal') do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <%= f.label :name, class: 'control-label' %>
  <%= f.text_field :name %>
  <%= f.file_field :video %>
  <%= f.submit "Add Video" %>
<% end %>

